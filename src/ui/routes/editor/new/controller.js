import Controller from '@ember/controller';
import EditorControllerMixin from "ghost-admin/src/utils/mixins/editor-base-controller/mixin";

export default Controller.extend(EditorControllerMixin, {

});
